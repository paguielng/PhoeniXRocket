from datetime import time
def Compteur(n) -> int:
    compteur = 0
    for i in range(n):
        compteur += 1
    return compteur

Détéction de la vibration : Détéction du décollage
    def decolfuse(
